{{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.type) }}
    {{ form_row(form.description) }}
    {{ form_row(form.generatorConfig) }}
    <input type="submit" value="Create">
{{ form_end(form) }}